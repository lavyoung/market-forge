spring:

  application:
    name: market-forge

  profiles:
    active: dev

server:
  port: 8080

mybatis:
  mapper-locations:
    - classpath*:mapper/*.xml

  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: info
