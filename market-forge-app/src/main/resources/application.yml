spring:

  application:
    name: market-forge

  profiles:
    active: dev

server:
  port: 8080

mybatis:
  config-location: classpath:mybatis/config/mybatis-config.xml

logging:
  level:
    root: info
