<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lavyoung.marketforge.infrastructure.persistent.dao.activity.IActivityOrderDao">

    <resultMap id="activityOrderResultMap"
               type="com.lavyoung.marketforge.infrastructure.persistent.po.activity.ActivityOrderPO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="activity_id" property="activityId"/>
        <result column="sku" property="sku"/>
        <result column="activity_name" property="activityName"/>
        <result column="strategy_id" property="strategyId"/>
        <result column="order_id" property="orderId"/>
        <result column="order_time" property="orderTime"/>
        <result column="total_count" property="totalCount"/>
        <result column="day_count" property="dayCount"/>
        <result column="month_count" property="monthCount"/>
        <result column="state" property="state"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="activityOrderColumns">
        id, user_id, activity_id, sku, activity_name, strategy_id, order_id, order_time,
        total_count, day_count, month_count, state, create_time, update_time
    </sql>

    <select id="queryByOrderId" resultMap="activityOrderResultMap">
        SELECT
        <include refid="activityOrderColumns"/>
        FROM raffle_activity_order
        WHERE order_id = #{orderId}
        LIMIT 1
    </select>

</mapper>
