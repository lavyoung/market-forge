<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lavyoung.marketforge.infrastructure.persistent.dao.IStrategyAwardDao">

    <resultMap id="strategyAwardResultMap" type="com.lavyoung.marketforge.infrastructure.persistent.po.StrategyAwardPO">
        <id column="id" property="id"/>
        <result column="strategy_id" property="strategyId"/>
        <result column="award_id" property="awardId"/>
        <result column="award_title" property="awardTitle"/>
        <result column="award_subtitle" property="awardSubtitle"/>
        <result column="award_count" property="awardCount"/>
        <result column="award_count_surplus" property="awardCountSurplus"/>
        <result column="award_rate" property="awardRate"/>
        <result column="rule_models" property="ruleModels"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="strategyAwardColumns">
        id, strategy_id, award_id, award_title, award_subtitle, award_count,
        award_count_surplus, award_rate, rule_models, sort, create_time, update_time
    </sql>

    <select id="queryStrategyAwardList" resultMap="strategyAwardResultMap">
        SELECT <include refid="strategyAwardColumns"/>
        FROM strategy_award
        WHERE strategy_id = #{strategyId}
        ORDER BY sort ASC
    </select>

    <select id="queryStrategyAward" resultMap="strategyAwardResultMap">
        SELECT <include refid="strategyAwardColumns"/>
        FROM strategy_award
        WHERE strategy_id = #{strategyId}
          AND award_id = #{awardId}
        LIMIT 1
    </select>

    <update id="updateByStrategyIdAndAwardId"
            parameterType="com.lavyoung.marketforge.infrastructure.persistent.po.StrategyAwardPO">
        UPDATE strategy_award
        SET award_title = #{awardTitle},
            award_subtitle = #{awardSubtitle},
            award_count = #{awardCount},
            award_count_surplus = #{awardCountSurplus},
            award_rate = #{awardRate},
            rule_models = #{ruleModels},
        sort = #{sort},
        update_time = CURRENT_TIMESTAMP
        WHERE strategy_id = #{strategyId}
          AND award_id = #{awardId}
    </update>

    <update id="decrementAwardCountSurplus">
        UPDATE strategy_award
        SET award_count_surplus = award_count_surplus - 1,
        update_time = CURRENT_TIMESTAMP
        WHERE strategy_id = #{strategyId}
          AND award_id = #{awardId}
          AND award_count_surplus &gt; 0
    </update>

</mapper>
