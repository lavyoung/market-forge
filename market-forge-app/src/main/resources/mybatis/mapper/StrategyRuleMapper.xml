<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lavyoung.marketforge.infrastructure.persistent.dao.IStrategyRuleDao">

    <resultMap id="strategyRuleResultMap" type="com.lavyoung.marketforge.infrastructure.persistent.po.StrategyRulePO">
        <id column="id" property="id"/>
        <result column="strategy_id" property="strategyId"/>
        <result column="award_id" property="awardId"/>
        <result column="rule_type" property="ruleType"/>
        <result column="rule_model" property="ruleModel"/>
        <result column="rule_value" property="ruleValue"/>
        <result column="rule_desc" property="ruleDesc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="strategyRuleColumns">
        id, strategy_id, award_id, rule_type, rule_model, rule_value,
        rule_desc, create_time, update_time
    </sql>

    <select id="queryStrategyRuleList" resultMap="strategyRuleResultMap">
        SELECT <include refid="strategyRuleColumns"/>
        FROM strategy_rule
        WHERE strategy_id = #{strategyId}
        ORDER BY id ASC
    </select>

    <select id="queryStrategyRule" resultMap="strategyRuleResultMap">
        SELECT <include refid="strategyRuleColumns"/>
        FROM strategy_rule
        WHERE strategy_id = #{strategyId}
        <choose>
            <when test="awardId == null">
                AND award_id IS NULL
            </when>
            <otherwise>
                AND award_id = #{awardId}
            </otherwise>
        </choose>
          AND rule_model = #{ruleModel}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.lavyoung.marketforge.infrastructure.persistent.po.StrategyRulePO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO strategy_rule
            (strategy_id, award_id, rule_type, rule_model, rule_value, rule_desc)
        VALUES
            (#{strategyId}, #{awardId}, #{ruleType}, #{ruleModel}, #{ruleValue}, #{ruleDesc})
    </insert>

    <update id="updateById" parameterType="com.lavyoung.marketforge.infrastructure.persistent.po.StrategyRulePO">
        UPDATE strategy_rule
        SET strategy_id = #{strategyId},
            award_id = #{awardId},
            rule_type = #{ruleType},
            rule_model = #{ruleModel},
            rule_value = #{ruleValue},
            rule_desc = #{ruleDesc}
        WHERE id = #{id}
    </update>

</mapper>
